sudo: required
language: generic

services: 
  - docker

before_install:
  - docker build -t complex -f ./client/Dockerfile.dev ./client
# script:
#   - docker run sumanth/dockernodereact npm test -- --coverage
after_success:
  - docker build -t multiclient ./client
  - docker build -t multinginx ./nginx
  - docker build -t multiserver ./server
  - docker build -t multiworker ./worker

  #log in to docker cli

  - echo "$DOCKER_PW" | docker login -u "$DOCKER_ID" --password-stdin docker.io

  - docker push sumanthgowda/multiclient
  - docker push sumanthgowda/multinginx
  - docker push sumanthgowda/multiserver
  - docker push sumanthgowda/multiworker
